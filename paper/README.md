# Paper Source

Main files:

- `main.tex`
- `references.bib`

Suggested local build:

```bash
pdflatex main.tex
bibtex main
pdflatex main.tex
pdflatex main.tex
```

This Codex environment does not currently have a LaTeX engine installed, so the source was sanity-checked structurally but not compiled here.
